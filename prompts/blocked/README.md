# Blocked phases

Empty at pipeline creation.

On phase failure, write `phase-X.md` here with:

- failure
- failing tests
- suspected cause
- attempted fixes
- remaining work

Then set `STATUS=BLOCKED` in `prompts/STATE.md` and **stop**. Do not auto-activate the next phase.
