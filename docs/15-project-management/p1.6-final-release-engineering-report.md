# P1.6 Final Release Engineering Report

**Date:** 2026-08-13  
**Role:** Release Engineer + CTO  
**Audit type:** Production release-candidate decision (Phases 0–16)  
**Git policy:** **NO COMMIT · NO PUSH · NO TAG**  

Evidence root: `$HOME/nexora-full-app-test/evidence/release-eng-audit/`

---

## 1. Executive verdict

### **YELLOW — READY WITH CONDITIONS**

| Layer | Status |
| --- | --- |
| **ENGINEERING STATUS** | **READY** — automated gates PASS on current dirty tree |
| **RELEASE ARTIFACT STATUS** | **BLOCKED** — no signed/notarized production Mac RC |
| **OPERATIONAL STATUS** | **PENDING** — escrow, OPS-01, training, incident brief |
| **HUMAN APPROVAL STATUS** | **PENDING** — CEO / CTO / pilot owner; backup policy PENDING CEO APPROVAL |
| **PILOT STATUS** | **NOT AUTHORIZED** for live café |

**Score: 73 / 100**

**Critical distinction:** Software tests passing ≠ ready for a live café. This audit confirms engineering readiness and **stops** at production signing.

---

## 2–4. Version / branch / cleanliness

| Field | Value |
| --- | --- |
| Version | **3.0.5** |
| Branch | `develop` |
| HEAD | `e236542` (`fix(recovery): fail closed on missing production database`) |
| Ahead of origin | **1** commit |
| Working tree | **DIRTY** |
| Clean RC baseline? | **NO** |

### Dirty production code (eventual release commit candidates)

- `main/routes/products.ts`, `main/routes/settings.ts`, `main/server.ts`
- `frontend/src/app/(dashboard)/products/page.tsx`, `settings/page.tsx`
- `frontend/src/components/products/ProductsTable.tsx`
- `frontend/src/lib/i18n/{en,es,pt}.json`
- `package.json`
- `tests/flo-settings.test.ts`, `tests/rec-01-recovery.test.ts`
- Untracked: `tests/product-tags-parse.test.ts`, `tests/shift-settings-api.test.ts`

### Dirty docs / memory (release pack)

- `.ai/*`, ops runbooks, P1.5/P1.6 audits, pilot-signoff/checklist, QA reports

Diff high-risk scan (`main/` + `frontend/`): **0** hits.

---

## 5–8. Artifact / signing / notarization

### PRODUCTION SIGNING = **BLOCKED**

| Check | Result |
| --- | --- |
| Keychain Developer ID identities | **0** |
| `CSC_LINK` / `APPLE_*` / notary env | **ALL UNSET** |
| Production signed build attempted | **NO — STOPPED** |
| Fabricated signature | **NO** |

Config (ready in repo/CI design, not on this Mac):

- Identity string: `Codify Apps Private Limited (BKDY677XJA)`
- `hardenedRuntime: true`, `notarize: true`, entitlements present
- Canonical path: `.github/workflows/release.yml` with `MAC_CERTS` + Apple API key secrets

### Existing local artifact (NOT for pilot)

| Field | Value |
| --- | --- |
| Path | `release/mac-arm64/Nexora.app` |
| Class | **TRAINING / QA (adhoc)** |
| Signature | `adhoc` · TeamIdentifier **not set** |
| SHA-256 (MacOS binary) | `34465676648bf5e5892e8f7791929be7ee01973a5317260f4f741ab7110cfef6` |
| Notarization | **NONE** |
| DMG/ZIP | **ABSENT** |

**Pilot artifact path:** *none produced*  
**Pilot checksum:** *n/a — BLOCKED*

---

## 9. Test environment

| Item | Value |
| --- | --- |
| Host | macOS arm64 (developer Mac) |
| Node | v24.x |
| Isolated destructive profile | Used only for prior TRAINING QA (not this signed-RC run) |
| Developer `flo-desktop` | **Untouched** (`mtime 2026-08-12 18:42:21`, epoch `1786540341` before=after Phase 2) |
| Port hygiene | Port 3001 freed before Phase 2 |

---

## 10–14. Test matrix & counts

### Phase 2 automated regression (executed this audit)

| Suite | Result |
| --- | --- |
| `npm run build` | **PASS** |
| `npm run test:security` | **PASS** |
| `npm run test:rec-01` | **PASS** |
| `npm run test:backup` | **PASS** |
| `npm run test:refunds` | **PASS** |
| `npm run test:financial-reporting` | **PASS** |
| `npm run test:shift-settings-api` | **PASS** |
| `npm test` | **PASS** |

Log: `evidence/release-eng-audit/phase2-regression.log` · exits: `phase2-exits.txt`

### Counts (release-engineering matrix)

| | Count |
| --- | --- |
| **PASS** | **14** |
| **FAIL** | **0** |
| **NOT VERIFIED** | **8** |
| **BLOCKED** | **6** |

**PASS includes:** build, security, rec-01, backup, refunds, financial-reporting, shift-settings, full test, developer-profile safety, FIN-01 automated semantics (via refunds suite §19), config hardenedRuntime/notarize present, TRAINING correctly labeled adhoc, pilot-scope docs forbid unsigned live use, diff secret scan clean.

**BLOCKED:** production signing, notarization, stapling, Gatekeeper on RC, exact signed artifact identity, signed-artifact smoke/REC/FIN/clean-machine.

**NOT VERIFIED:** clean physical Mac install, printer hardware, Drive OAuth E2E, new-machine JWT, formal soak, LAN café hardware, OS file-picker E2E, café human briefs.

---

## 15–20. Domain results

| Domain | Result | Notes |
| --- | --- | --- |
| **Security** | **PASS** (automated) | Suites green; no weakened controls |
| **Financial / FIN-01** | **PASS** (automated) | Non-cash over remaining rejects; cash change-by-design; collectible gate closed |
| **REC-01** | **PASS** (automated) | Suite green; **signed-artifact REC smoke BLOCKED** |
| **Backup/restore** | **PASS** (automated) | Continuity + production restore suites in `test:backup` |
| **KDS / network** | **PASS** (unit/integration) · café LAN **NOT VERIFIED** | Recovery skips KDS/Server App/mDNS (suite) |
| **Printer** | **NOT VERIFIED** | No hardware |
| **Clean machine** | **NOT VERIFIED** | No separate clean Mac; must not claim PASS on developer host |
| **Signed-artifact smoke** | **BLOCKED** | No production artifact |
| **Signed-artifact REC-01** | **BLOCKED** | No production artifact |
| **Signed-artifact FIN-01** | **BLOCKED** | No production artifact |

TRAINING/QA packaged drills (prior evidence under `$HOME/nexora-full-app-test`, `$HOME/nexora-p1-5-dr-final`, etc.) remain valid for **engineering** confidence only — **not** substitutes for signed RC verification.

---

## 21–23. Clean-machine / RTO-RPO / human gates

| Gate | Status |
| --- | --- |
| Clean-machine install of **exact signed** RC | **NOT VERIFIED** / **BLOCKED** (no signed RC) |
| P1.5 DR RTO/RPO (engineering) | **PASS** historically (RTO 6.12 min / RPO 18 s) — TRAINING profile |
| Master PIN escrow | **PENDING** |
| OPS-01 | **PENDING** |
| Owner/manager training | **PENDING** |
| Incident process brief | **PENDING** |
| Backup frequency / local / Drive retention | **PENDING CEO APPROVAL** |
| RTO/RPO policy approval | **PENDING CEO APPROVAL** (drill numbers ≠ approved policy) |
| CEO / CTO / pilot owner signatures | **PENDING** |

---

## 24. Release blockers (exact)

1. **No Developer ID / notarization credentials on this Mac** → cannot build production RC.  
2. **Dirty `develop` tree** → not a clean tagged release baseline.  
3. **No signed/notarized DMG/ZIP** → nothing eligible for live café.  
4. **Human gates incomplete** (escrow, OPS-01, training, approvals).

---

## 25. Findings by severity

### P0

1. Production signing/notarization unavailable → pilot binary cannot be produced here.  
2. Working tree not clean → RC identity undefined until commit/tag (user-authorized later).

### P1

1. Master PIN offline escrow PENDING.  
2. OPS-01 PENDING.  
3. Backup/retention/RTO-RPO policy PENDING CEO APPROVAL.  
4. Training + incident understanding PENDING.  
5. CEO/CTO/pilot sign-off PENDING.  
6. WIN-01 if Windows café is later chosen.

### P2

1. Historical doc drift remains in older registers (M6/TD-05/P1.5 discovery bottoms).  
2. Uncommitted INV-TAGS / shift-settings product changes not yet in a release tag.  
3. QA-FIN01-STATUS-01 display semantics (P2).

### P3

1. Printer / OAuth / soak / new-machine JWT / file-picker OS dialog.

---

## 26. Exact next actions

1. Load **Developer ID Application** + App Store Connect API key (or run CI `release.yml` with secrets).  
2. User-authorize: commit dirty production+docs into clean tree; tag `v3.0.5-rc.N`.  
3. Build notarized Mac `dmg`/`zip`; verify `codesign` / `spctl` / `stapler`.  
4. Record SHA-256 of **that** artifact; smoke + REC-01 + FIN-01 on it in **fresh isolated** userdata.  
5. Complete `docs/13-operations/pilot-signoff.md` human gates.  
6. Only then consider live-café authorization.

---

## 27. Final release recommendation

**Do not sign off P1.6 for live pilot.**  
**Do not ship** `release/mac-arm64/Nexora.app` to a café.  
**Do** treat engineering as READY WITH CONDITIONS and unblock signing credentials next.

| Question | Answer |
| --- | --- |
| CAN P1.6 BE SIGNED OFF | **NO** |
| CAN THIS BUILD BE USED FOR A LIVE CAFÉ | **NO** |
| CEO approval required | **YES** (PENDING) |
| CTO approval required | **YES** (PENDING) |

---

## Platforms in pilot scope

**macOS** required for first café framing.  
**Windows / Linux:** NOT APPLICABLE until café platform chosen (then signed Win / keyring gates apply).

---

## Git status at end (no commit)

```
## develop...origin/develop [ahead 1]
 M  (docs, .ai, main/, frontend/, package.json, tests…)
?? (pilot docs, QA reports, new tests…)
```

User must separately authorize any commit/tag/push.

---

## STOP

Production signing not faked.  
Developer `flo-desktop` not touched.  
NO COMMIT. NO PUSH. NO TAG.
