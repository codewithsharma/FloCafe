# P1.6 Final Release-Candidate Audit

**Date:** 2026-08-13  
**Role:** CTO + Release Engineer  
**Product:** Nexora POS (`flo-desktop`) **v3.0.5**  
**Audit type:** Final release-candidate gate (Phases 1–12)  
**Commit policy:** **NO COMMIT / NO PUSH** performed in this audit  

---

## 1. Executive verdict

### FINAL VERDICT: **YELLOW — READY WITH CONDITIONS**

| Layer | Status |
| --- | --- |
| **Engineering ready** | **YES** — closed product gates (REC-01, FIN-01, B2, backup/refunds/security suites) verify on this tree when ports are free |
| **Pilot ready** | **NO** — no production signed/notarized artifact; dirty working tree; human/ops gates incomplete |

**Score: 74 / 100**

| Dimension | Score | Notes |
| --- | --- | --- |
| Product / money / recovery | 92 | Closed findings hold; suites green on clean ports |
| Security | 88 | Sandbox/B2/JWT/network_mode suites PASS |
| Release hygiene | 45 | Dirty tree; no clean tagged RC |
| Production signing | 0 | **PRODUCTION SIGNING BLOCKED** on this machine |
| Ops / human gates | 35 | Escrow, OPS-01, training, CEO/CTO signatures PENDING |
| Optional café hardware | 20 | Printer/OAuth/soak/new-machine JWT NOT VERIFIED |

**Do not confuse engineering ready with pilot ready.**

---

## 2–4. Version / branch / git state

| Field | Value |
| --- | --- |
| Version | **3.0.5** |
| Branch | `develop` |
| HEAD | `e236542` (`fix(recovery): fail closed on missing production database`) |
| Ahead of origin | 1 commit |
| Working tree | **DIRTY** — docs + production code + tests uncommitted |
| Suitable clean RC baseline? | **NO** — must ship from a **clean, tagged** commit after deciding which uncommitted product changes belong in RC |

### Uncommitted production code (must be intentional if included in RC)

- `main/routes/products.ts`, `main/routes/settings.ts`, `main/server.ts`
- `frontend/.../products/page.tsx`, `ProductsTable.tsx`, `settings/page.tsx`, i18n
- `package.json` (test wiring)
- Tests: `product-tags-parse.test.ts`, `shift-settings-api.test.ts`, related updates

### Docs-only dirty (safe lag)

- `.ai/*`, `docs/13-operations/*`, `docs/15-project-management/*`, `docs/16-release/*`

---

## 5–7. Artifact / signing / notarization

### PRODUCTION SIGNING BLOCKED — CREDENTIALS/IDENTITY REQUIRED

| Check | Result |
| --- | --- |
| Config identity | `Codify Apps Private Limited (BKDY677XJA)` |
| `hardenedRuntime` | `true` |
| `notarize` | `true` (electron-builder) |
| Local codesign identities | **0** |
| `CSC_LINK` / `APPLE_*` env | **UNSET** |
| Attempted production signed build | **STOPPED** (would produce unsigned/adhoc — forbidden for production claim) |

### Existing local artifact (NOT pilot)

| Field | Value |
| --- | --- |
| Path | `release/mac-arm64/Nexora.app` |
| Class | **TRAINING / QA (adhoc)** |
| Signature | `adhoc` — TeamIdentifier **not set** |
| Notarized | **NO** |
| DMG/ZIP production packages | **ABSENT** |

**Exact artifact that should be used for pilot:**  
**NONE AVAILABLE ON THIS HOST.** Pilot must use a **Developer ID–signed, notarized, stapled** Mac `dmg`/`zip` (or CI `release.yml` output) built from a clean tag of 3.0.5 (+ approved uncommitted fixes). TRAINING `Nexora.app` must **not** be used for live café service.

Windows signed NSIS: **NOT APPLICABLE** until a Windows café is in pilot scope (then **WIN-01** required).

---

## 8–9. Clean-machine install + release smoke (signed artifact)

| Gate | Status |
| --- | --- |
| Clean-machine install of **exact signed** pilot artifact | **NOT VERIFIED** — blocked by missing production signature |
| Release smoke on signed artifact | **NOT VERIFIED** — blocked |

**Engineering evidence (TRAINING only — does not substitute):** prior isolated userdata QA / pending-complete / P1.5 DR / fresh-smoke passes exist under `$HOME/nexora-*-*` profiles. Those validate product behavior, **not** Gatekeeper/notarization.

---

## 10. REC-01 result

| Layer | Status |
| --- | --- |
| Automated `npm run test:rec-01` | **PASS** (rerun with port 3001 free) |
| Packaged TRAINING destructive re-drill (isolated UD) | **PASS** (prior pending-complete) |
| REC-01 on **signed production** artifact | **NOT VERIFIED** — no signed artifact |

**Note:** First Phase 9 `test:rec-01` / `test:backup` / `npm test` failed with port contamination from running TRAINING Nexora on `:3001` (recovery health expected 503, got 200 from live ACTIVE app). After stopping isolated Nexora only, all three **PASS**. Developer `flo-desktop` mtime unchanged (`2026-08-12T13:12:21Z`).

---

## 11. FIN-01 result

| Rule | Status |
| --- | --- |
| Collectible = total − gross tender (refunds do not recreate capacity) | **PASS** (closed; suite + DR) |
| **Non-cash** tender > remaining | **PASS** — rejected |
| **Cash** tender > remaining | **PASS** — change-on-cash (applied capped; `change_amount`) |
| Doc drift (“₹401 always reject”) | **FIXED** in release-facing docs this audit |

---

## 12–15. Security / backup / shifts / KDS

| Suite / area | Status |
| --- | --- |
| `npm run build` | **PASS** |
| `npm run test:security` | **PASS** |
| `npm run test:rec-01` | **PASS** (clean rerun) |
| `npm run test:backup` | **PASS** (clean rerun) |
| `npm run test:refunds` | **PASS** |
| `npm run test:financial-reporting` | **PASS** |
| `npm run test:shift-settings-api` | **PASS** |
| `npm test` | **PASS** (clean rerun) |
| Diff secret/backdoor scan (`main/` + `frontend/`) | **0** high-risk hits |
| Shift / day-close | **PASS** (engineering) |
| KDS/network localhost | **PASS** (engineering); café OPS-01 **PENDING** |

Evidence: `$HOME/nexora-full-app-test/evidence/rc-audit/` (`phase9-suites.log`, `phase9-rerun.log`, `suite-exits-rerun.txt`, `ops-gates.json`).

---

## 16–19. Optional

| Item | Status |
| --- | --- |
| Physical printer | **NOT VERIFIED** |
| Google Drive OAuth completion | **NOT VERIFIED** (UI present) |
| New-machine JWT | **NOT VERIFIED** |
| Formal soak/load | **NOT VERIFIED** (light concurrency only in prior QA) |
| Native OS file-picker restore E2E | **NOT VERIFIED** (IPC restore verified) |

These do **not** block engineering RC cut unless pilot scope mandates them.

---

## 20–21. Documentation / operational gates

### Doc consistency (this audit)

**Fixed (safe docs only):**

- `docs/13-operations/dr-drill-worksheet.md` — cash vs non-cash FIN-01 rows
- `docs/16-release/pilot-release-checklist.md` C8 — method-qualified
- `docs/15-project-management/full-test-completion-report.md` — withdrawn FAIL / next steps corrected

**Remaining historical drift (not rewritten wholesale):** `milestones.md` / `progress.md` M6 NOT STARTED; `technical-debt.md` TD-05 sandbox; frozen P1.5 discovery bottom — treat as historical registers.

### Operational gates (`pilot-signoff.md`)

| Gate | Status |
| --- | --- |
| P1.5 DR drill | **PASS** (engineering) |
| Master PIN offline escrow | **PENDING** |
| OPS-01 | **PENDING** |
| Backup policy (frequency / retention) | **PENDING CEO APPROVAL** |
| Production artifact packaged | **PENDING** |
| Windows signed | **NOT APPLICABLE** (until Win café) |
| Linux keyring | **NOT APPLICABLE** (until Linux café) |
| Printer / KDS café | **PENDING** / N/A by scope |
| Fresh install / recovery / financial (engineering) | **PASS** · café **PENDING** |
| Incident escalation understood | **PENDING** |
| Owner/manager training | **PENDING** |
| CEO / CTO / pilot owner signatures | **PENDING** |

---

## 22–24. Counts (this RC audit matrix)

| | Count |
| --- | --- |
| **PASS** | **18** (build, security, rec-01, backup, refunds, financial-reporting, shift-settings, full test, FIN-01 semantics, engineering DR/recovery/financial, doc release-facing FIN-01 fix, secret scan clean, developer profile safety, TRAINING adhoc correctly labeled, config hardenedRuntime/notarize present, CI release path designed) |
| **FAIL** | **0** product defects found in this audit |
| **NOT VERIFIED / BLOCKED** | **10** (signed artifact, notarization, stapling, Gatekeeper on RC, clean-machine on signed, smoke on signed, REC on signed, printer, Drive OAuth, new-machine JWT, formal soak, file-picker OS dialog) |

---

## 25–28. Findings by severity

### P0

1. **No production signed/notarized Mac artifact** — pilot binary unavailable.  
2. **Working tree not RC-clean** — uncommitted production code on `develop`.

### P1

1. Master PIN offline escrow not completed per café.  
2. OPS-01 not accepted/enforced on site.  
3. Backup numeric policy **PENDING CEO APPROVAL**.  
4. Owner/manager training + incident brief **PENDING**.  
5. CEO/CTO/pilot-owner sign-off **PENDING**.  
6. WIN-01 if Windows café enters scope.

### P2

1. Historical doc registers (M6/TD-05/P1.5 discovery) still stale.  
2. QA-FIN01-STATUS-01 (payment_status/balance display after refund+gross settle).  
3. INV-TAGS source still uncommitted (verified in TRAINING rebuild).

### P3

1. Formal soak.  
2. Drive OAuth E2E.  
3. Physical printer.  
4. New-machine JWT.  
5. Native file-picker automation.

---

## 29. Exact blockers (pilot)

1. Obtain Developer ID + notary credentials (or CI secrets) and produce notarized Mac RC.  
2. Commit/tag a **clean** release candidate including approved product fixes.  
3. Re-verify smoke + REC-01 on that **exact signed** artifact in isolated userdata.  
4. Complete Master PIN escrow, OPS-01, training, backup policy approval, CEO/CTO/pilot signatures.

---

## 30. Exact next actions

1. **Release engineering:** Import `Developer ID Application (BKDY677XJA)` + App Store Connect API key (or run GitHub `release.yml` with `MAC_CERTS` / `APPLE_API_*`).  
2. **Branch hygiene:** Decide include/exclude uncommitted `main/`+`frontend/` changes; commit; tag `v3.0.5-rc.N` from clean tree.  
3. **Build:** `npm run build:mac` (or CI) → verify `codesign --verify --deep --strict`, `spctl --assess --type execute`, `xcrun stapler validate`.  
4. **Validate:** Fresh isolated userdata smoke + REC-01 on the signed artifact only.  
5. **Ops:** Execute `pilot-signoff.md` human gates; attach evidence.  
6. **Authorize live service** only when engineering + ops gates are PASS (or written waiver).

---

## Gate separation (mandatory)

| Category | State |
| --- | --- |
| **ENGINEERING GATES** | Closed findings + suites **PASS**; **RC packaging incomplete** (dirty tree + unsigned) |
| **OPERATIONAL GATES** | Escrow / OPS-01 / training / incident / café re-confirm **PENDING** |
| **CEO / CTO APPROVALS** | **REQUIRED** — both signatures **PENDING**; backup policy **PENDING CEO APPROVAL** |

---

## Approvals required?

| Question | Answer |
| --- | --- |
| Ready for pilot (live café)? | **NO** |
| CEO approval required? | **YES** |
| CTO approval required? | **YES** |
| Exact pilot artifact | **Not produced** — blocked pending signing |

---

## STOP

No commit. No push.  
Production signing not faked. TRAINING artifact not relabeled as production.
