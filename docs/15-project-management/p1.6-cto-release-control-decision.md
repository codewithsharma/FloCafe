# P1.6 CTO Release Control Decision

**Date:** 2026-08-13  
**Role:** Release Engineer + CTO  
**Evidence:** `$HOME/nexora-full-app-test/evidence/release-eng-cto-20260813T044621Z/`  

**NO COMMIT · NO PUSH · NO TAG**

---

## 1. RELEASE STATUS

| Field | Value |
| --- | --- |
| Status | **YELLOW — READY WITH CONDITIONS** |
| READY FOR PILOT | **NO** |
| Score | **73 / 100** |

| Layer | Status |
| --- | --- |
| ENGINEERING | **VERIFIED READY** (fresh suites PASS) |
| RELEASE ARTIFACT | **BLOCKED** — no signed/notarized RC |
| OPERATIONAL / HUMAN | **PENDING** |
| PILOT AUTHORIZATION | **DENIED** until signed RC + human gates |

---

## 2. VERIFIED

### Engineering (executed this session)

| Suite | Result | Duration |
| --- | --- | --- |
| `npm run build` | **PASS** | 7s |
| `npm run test:security` | **PASS** | 33s |
| `npm run test:rec-01` | **PASS** | 2s |
| `npm run test:backup` | **PASS** | 4s |
| `npm run test:refunds` | **PASS** | 3s |
| `npm run test:financial-reporting` | **PASS** | 1s |
| `npm run test:shift-settings-api` | **PASS** | 2s |
| `npm test` | **PASS** | 148s |

### Product / prior closed gates (not re-opened)

- REC-01 fail-closed recovery — CLOSED  
- FIN-01 gross-tender / cash-change vs non-cash reject — CLOSED  
- P1.5 DR drill — PASS (RTO 367s / RPO 18s)  
- Security / backup / refunds / shifts — PASS  

### Safety

- Developer profile `~/Library/Application Support/flo-desktop` **untouched** (mtime unchanged)  
- TRAINING artifact correctly identified as **adhoc** (not production)  
- CI release workflow **exists** and defines signed Mac path with required secrets  

### Pre-flight facts

- Version **3.0.5**, branch `develop`, HEAD `e236542`, ahead of origin by 1  
- Working tree **dirty** (25 modified + 15 untracked)  
- Production Mac command when unblocked: `npm run build:mac` or CI `release.yml` → `release-mac`  

---

## 3. BLOCKED / NOT VERIFIED

### Technical blockers

| Item | Classification |
| --- | --- |
| **PRODUCTION SIGNING BLOCKED** — 0 codesign identities; all `CSC_*` / `APPLE_*` env UNSET | **TECHNICAL BLOCKER** |
| Notarization / stapling / Gatekeeper on RC | **BLOCKED** (depends on signing) |
| Exact signed/notarized Mac DMG/ZIP | **BLOCKED** — not produced |
| Exact-artifact smoke / REC-01 / FIN-01 on signed RC | **BLOCKED** — no signed artifact |
| Clean tagged RC baseline | **BLOCKED** until user-authorized commit/tag |
| Clean physical machine install of signed RC | **NOT VERIFIED** |

### Signing path decision (A / B / C)

| Option | Status |
| --- | --- |
| **A.** Credentials on this Mac | **Unavailable** |
| **B.** CI `release.yml` produces signed artifact | **Configured in code**; requires GitHub secrets `MAC_CERTS`, `MAC_CERTS_PASSWORD`, `APPLE_API_KEY`, `APPLE_API_KEY_ID`, `APPLE_API_ISSUER` — **NOT VERIFIED** that secrets are populated remotely |
| **C.** Release remains blocked | **Current state** until A or B succeeds |

**No further local production-signing attempts** until credentials appear.

### Human / field gates (PENDING — not FAIL)

- Master PIN offline escrow  
- OPS-01 enforcement on site  
- Owner/manager training  
- Incident response briefing  
- Backup frequency / local retention / Drive retention — **PENDING CEO APPROVAL**  
- RTO/RPO policy approval — **PENDING CEO APPROVAL**  
- Printer hardware — **NOT VERIFIED**  
- Café LAN/KDS — **NOT VERIFIED**  
- Signed Windows — **NOT APPLICABLE** until Windows café  
- Linux keyring — **NOT APPLICABLE** until Linux café  
- CEO / CTO / pilot-owner signatures — **PENDING**  

### Training artifact (explicit)

`release/mac-arm64/Nexora.app` — Signature=**adhoc**, TeamIdentifier unset, SHA-256 binary `34465676648bf5e5892e8f7791929be7ee01973a5317260f4f741ab7110cfef6` — **MUST NOT** be used for live café.

---

## 4. RECOMMENDED NEXT ACTION

**Smallest safe next action:**

1. **Provide production signing** via either:
   - install Developer ID + notary env on this Mac, **or**
   - confirm GitHub Actions secrets for `release.yml` and cut a signed Mac release from a **clean tagged** commit  
2. **User-authorize** a clean commit of the proposed RC baseline (see `03-proposed-rc-baseline.md` in evidence) — then tag `v3.0.5-rc.1`.  
3. Verify the CI/local signed artifact with `codesign` / `spctl` / `stapler`.  
4. Run exact-artifact smoke + REC-01 in `$HOME/nexora-release-test-<timestamp>` only.  
5. Complete human gates in `docs/13-operations/pilot-signoff.md`.

Until step 1–3 succeed, **P1.6 cannot be signed off for pilot** and **no build on this Mac is live-café eligible**.

---

## Proposed RC commit (awaiting user authorization)

See evidence `03-proposed-rc-baseline.md`. **Not executed.**

---

NO COMMIT. NO PUSH. NO TAG.
